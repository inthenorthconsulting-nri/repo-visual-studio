name: Environment Deploy
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: npm run build
  deploy-production:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://example.com
    steps:
      - run: npm run deploy
